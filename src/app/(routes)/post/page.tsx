import CreatePost from "@/components/CreatePost";
import Container from "@/components/ui/container";

const page = () => {
  return (
    <Container>
      <CreatePost />
    </Container>
  );
};
export default page;
